import React from 'react';

export const NoteForm: React.FC = () => {
    return (
        <form>
            <input type='text' />
            <button type='submit'>Add note</button>
        </form>
    )
}